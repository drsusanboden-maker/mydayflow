<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>My Day Flow</title>
  <meta name="description" content="Gentle daily routine with checkboxes, weekly summary, gentle banners and reflection." />
  <style>
    :root{
      --bg: #eaf6ff; /* light sky blue */
      --card: #ffffff;
      --accent: #68b8ff; /* sky blue */
      --coral: #ffb3a7;
      --lav: #d9c8ff;
      --muted: #6b7280;
      --glass: rgba(255,255,255,0.8);
      --radius:14px;
      --maxw:720px;
      font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    }
    html,body{height:100%;margin:0;background: linear-gradient(180deg,var(--bg),#f7fbff);color:#07203a}
    .wrap{max-width:var(--maxw);margin:18px auto;padding:16px}
    .header{display:flex;gap:12px;align-items:center}
    .logo{width:62px;height:62px;border-radius:16px;background:linear-gradient(135deg,var(--accent),var(--lav));display:flex;align-items:center;justify-content:center;box-shadow:0 10px 30px rgba(8,44,92,0.08)}
    .logo strong{color:white;font-size:22px;letter-spacing:0.6px}
    h1{margin:0;font-size:20px}
    .quote{margin-top:8px;color:var(--muted);font-size:14px;line-height:1.3}


    .card{background:var(--card);border-radius:var(--radius);padding:14px;margin-top:14px;box-shadow:0 10px 30px rgba(8,44,92,0.06)}
    .section-title{display:flex;justify-content:space-between;align-items:center;margin-bottom:10px}
    .tasks{display:grid;gap:10px}
    .task{display:flex;align-items:center;gap:12px;padding:10px;border-radius:12px;background:linear-gradient(180deg,var(--glass),#ffffff)}
    .task input{width:18px;height:18px}
    .task label{flex:1}
    .small{font-size:13px;color:var(--muted)}
    .mutebtn{background:transparent;border:1px solid rgba(7,32,58,0.06);padding:8px 10px;border-radius:10px}


    .progressWrap{display:flex;gap:12px;align-items:center}
    .progress{flex:1;background:linear-gradient(90deg,#f3f9ff,#f8fbff);border-radius:999px;height:12px;overflow:hidden;border:1px solid rgba(7,32,58,0.03)}
    .progress > i{display:block;height:100%;background:linear-gradient(90deg,var(--accent),var(--lav));width:0%}


    .controls{display:flex;gap:8px;align-items:center}
    .btn{background:linear-gradient(90deg,var(--coral),#ffd7d0);border-radius:10px;padding:8px 12px;border:0;color:#082335;font-weight:600;box-shadow:0 6px 18px rgba(255,179,167,0.11)}
    .settings{display:flex;flex-direction:column;gap:8px;margin-top:8px}
    .row{display:flex;gap:8px;align-items:center}
    input[type=time]{padding:6px;border-radius:8px;border:1px solid #e9f2ff;background:white}
    .footer{margin-top:14px;font-size:12px;color:var(--muted);text-align:center}


    /* gentle banner */
    .banner{position:fixed;left:50%;transform:translateX(-50%);bottom:18px;background:linear-gradient(90deg,var(--accent),var(--lav));color:white;padding:12px 18px;border-radius:999px;box-shadow:0 18px 40px rgba(8,44,92,0.12);display:flex;gap:12px;align-items:center;z-index:999;max-width:92%;font-weight:600}
    .banner .close{background:transparent;border:0;color:rgba(255,255,255,0.9);cursor:pointer;font-weight:700}
    .reflection{margin-top:12px}
    textarea{width:100%;min-height:70px;border-radius:10px;border:1px solid #eef6ff;padding:8px;font-family:inherit;resize:vertical}
    @media (max-width:430px){.logo{width:54px;height:54px}.logo strong{font-size:18px}}
  </style>
</head>
<body>
  <div class="wrap">
    <div class="header">
      <div class="logo"><strong>MD</strong></div>
      <div>
        <h1>My Day Flow</h1>
        <div class="quote" id="quote">You’re not alone — even small steps matter.</div>
      </div>
    </div>


    <div class="card">
      <div class="section-title">
        <strong>Today</strong>
        <div class="small" id="todayDate"></div>
      </div>


      <div id="tasksList" class="tasks">
        <!-- tasks inserted via JS -->
      </div>


      <div style="margin-top:12px;display:flex;justify-content:space-between;align-items:center">
        <div class="small">Completed anchors: <span id="anchorsDone">0</span>/2</div>
        <div class="controls">
          <button class="btn" id="saveBtn">Save</button>
          <button class="mutebtn" id="resetBtn">Reset</button>
        </div>
      </div>
    </div>


    <div class="card">
      <div class="section-title"><strong>Weekly Summary</strong><div class="small">Last 7 days</div></div>
      <div style="display:flex;gap:12px;align-items:center">
        <div style="width:64px;text-align:center">
          <div style="font-weight:700;font-size:18px" id="weekCount">0</div>
          <div class="small">days</div>
        </div>
        <div class="progress"><i id="progressBar"></i></div>
        <div class="small" id="weekPercent">0%</div>
      </div>
    </div>


    <div class="card">
      <div class="section-title"><strong>Reminders & Settings</strong><div class="small">Banner reminders (in-app)</div></div>
      <div class="settings">
        <div class="row">
          <label class="small" style="width:120px">Morning (walk)</label>
          <input type="time" id="timeMorning" value="08:00">
        </div>
        <div class="row">
          <label class="small" style="width:120px">Study (Italian)</label>
          <input type="time" id="timeStudy" value="14:00">
        </div>
        <div class="row">
          <label class="small" style="width:120px">Evening (wind-down)</label>
          <input type="time" id="timeEvening" value="21:00">
        </div>
        <div style="display